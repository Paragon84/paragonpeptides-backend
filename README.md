# Paragon Peptides Backend

Backend API for paragonpeptides.net

## Features
- Inventory management (connected to Supabase)
- x402 USDC payments
- Digital order codes

## Setup
1. Copy `.env.example` to `.env` and add your Supabase keys
2. Run `npm install`
3. Run `npm run dev`